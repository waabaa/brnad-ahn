# GEO(AI 검색) 점검 — brandatlas.co.kr

> 측정 2026-09-08 · 대상 브랜드 1,767(색인 1,743) + 허브 42 + 홈
> 기준: Google AI 최적화 가이드(2026-05 공개, 06 보완) — "생성형 AI 검색 최적화는 여전히 SEO다"
> 직전 측정 2026-09-01(준비도 65) 대비 변화를 함께 적는다.

## GEO 준비도 76 / 100 (이전 65)

기술 접근성은 이전 회차에 이미 해결됐고, 이번 회차에서 오른 것은 **질의에 답할 수 있는
표면**이다. 남은 병목은 둘로 좁혀졌다 — 로고 215건과, 근거가 없어 기원 국가를 적지 못하는
607건. 외부 언급(위키백과·레딧 등재)은 이전 회차와 같이 착수하지 않았다.

| 항목 | 가중치 | 이전 | 현재 | 근거 |
|---|---|---|---|---|
| 기술 접근성 | 20% | 92 | 92 | 전 페이지 정적 HTML, AI 크롤러 12종 허용, 로그로 수집 확인 |
| 구조적 가독성 | 20% | 70 | **90** | 질문형 H2 100%, 팩트 표 100%, FAQPage 100% |
| 인용 가능성 | 25% | 62 | **75** | 정의문 리드 90%, 자체 완결 Q&A 8,400여 개 |
| 멀티모달 | 15% | 55 | **68** | 로고 이미지 86%, 이미지 sitemap |
| 권위·브랜드 신호 | 20% | 45 | **55** | sameAs 687건, author/publisher 선언, 출처 절 |

---

## 1. 질의별로 답할 수 있는 비율

유입은 사이트명이 아니라 브랜드명으로 온다는 전제는 그대로다. 그 전제에서 유일하게
의미 있는 지표는 "이 질의가 들어왔을 때 답이 실린 페이지가 몇 개인가"이다.

| 질의 유형 | 2026-09-01 | 2026-09-08 | 비고 |
|---|---|---|---|
| `OO 브랜드` `OO 뜻` | 1,302 (90%) | 1,541 (100%) | 정의문·개요는 전 페이지 보유 |
| `OO 로고` | 1,024 (71%) | 1,326 (**86%**) | 로컬 자산화 + 공식 사이트 수집 |
| `OO 설립연도` | 527 (36%) | 960 (**62%**) | 위키데이터 P571(P856 일치 개체) |
| `OO 어느 나라` | 527 (36%) | 934 (**61%**) | 위키데이터 P17 |
| `OO 창업자` `OO 모기업` `OO 본사` | 표 없음 | 팩트 표 100% | 값이 있는 칸만 노출 |
| `OO BI/CI` | 326 (22%) | 212 | 파일 없는 항목을 정리해 실측치가 내려감 |

모수가 1,449 → 1,541로 늘어난 상태에서 비율이 올랐다.

## 2. 이번 회차에 고친 것

**FAQ 결손 162건 → 0.** FAQ가 2개 미만이라 `FAQPage`가 붙지 않던 페이지가 색인 대상의
10%였다. 원인은 근거 부족이 아니라 근거를 안 쓴 것이었다 — 산업 분류(자체 검수값)는
기원 국가가 있을 때만 답변에 실렸고, 아카이브 계열 레코드의 `designer:` 필드는 아예
쓰이지 않았다. 두 근거를 FAQ 항목으로 추가해 **FAQPage 커버리지 85% → 100%**가 됐다.
새 사실은 만들지 않는다는 원칙은 그대로다 — 둘 다 기존 필드 인용이다.

**인물 섹션 중복 렌더.** 한 레코드에 `창업자: X; CEO: Y; 창업자: X`처럼 같은 값이 여러 벌
들어와 본문과 JSON-LD에 그대로 반복됐다. 렌더 단계에서 `키: 값` 항목 단위로 중복을
제거한다. 데이터 정정이 아니라 렌더 방어이므로 같은 형태가 다시 들어와도 재발하지 않는다.

**팩트 재수집.** P856 완전 일치로 확정된 690개 개체에서 국가 632·설립 645·본사 592·
창업자 246·모기업 218을 다시 받아 커버리지를 회복했다.

## 3. 기술 접근성 92 — 변동 없음

AI 크롤러는 자바스크립트를 실행하지 않는다. 전 페이지가 정적 HTML이고 본문이 JS 없이
그대로 읽힌다. robots.txt에 GPTBot·OAI-SearchBot·ClaudeBot·PerplexityBot·Google-Extended·
Applebot-Extended 등 12종을 명시 허용했다. 직전 회차 nginx 로그에서 GPTBot 893회(200 응답
98%)로 네이버 Yeti보다 자주 왔다.

`llms.txt`는 구글이 순위·노출에 영향 없음을 명시했으므로 비구글 AI 시스템용 안내로만 둔다.
RSL 1.0은 도입하지 않았다(실익 불분명).

## 4. 권위·브랜드 신호 55 — 여전히 최저

```
브랜드 페이지 author / publisher      1,541 / 1,541   Organization 선언
Organization + Article + Breadcrumb   1,541
FAQPage                               1,541 (100%)
개별 브랜드 Wikidata sameAs             687 (45%)
dateModified                          실제 본문 변경분만 갱신
자체 브랜드의 위키백과·레딧·유튜브 언급    0
```

Ahrefs 연구에서 브랜드 언급은 백링크보다 AI 인용과 3배 강하게 상관하지만(0.737 vs 0.266),
이는 **유입 병목이 아니라 인용 자격의 문제**다. 사람들은 사이트 이름으로 검색하지 않는다.
이전 회차 판단대로 외부 언급 만들기는 착수하지 않는다.

`dateModified`를 본문이 실제로 바뀐 페이지에만 찍는 방식은 유지한다. 3개월 이내 콘텐츠가
AI 인용률 약 3배라는 이유로 매 빌드 오늘 날짜를 찍으면 신호 자체가 무효화된다.

## 5. 남은 병목 2개

**로고 215건.** `OO 로고`는 두 번째로 큰 질의이고 이미지 검색이 실제 유입원이다.
자동 채택은 금지(오매칭 이력) — 후보를 모아 육안 검증하는 방식을 유지한다.

**기원 국가 미확인 607건.** 이 가운데 580건은 공식 웹사이트·위키데이터 개체가 모두 없어
현재 규칙(정의문 근거 또는 P856 완전 일치)으로는 채울 근거가 없다. 커버리지를 위해
이름 유사도 매칭을 도입하지 않는다 — 잘못된 `sameAs`는 검색엔진에 "이 페이지는 다른
회사를 설명한다"고 선언하는 것과 같다.

## 6. 네이버 '리다이렉션된 페이지' 진단 (2026-09-08 반영)

웹마스터 도구가 3xx로 수집한 108건을 전수 검증했다.

```
301 → 200 한 홉        107 / 107      체인·루프 없음
매핑 정확도            107 / 107      대상 레코드 id 일치
sitemap·rss·llms·검색인덱스 잔존   0
내부 링크 잔존                     0
목적지가 noindex                  41      ← 유일한 실제 결함
```

리다이렉트 자체는 건강했고 사이트 어디에도 구 URL이 남아 있지 않았다. 문제는 **41건이
301을 따라 도착한 곳이 `noindex`**였다는 것이다. 크롤러는 구 URL을 계속 확인하는데
새 URL은 색인될 수 없어 교체가 진행되지 않는다.

원인은 디렉토리 등급(한글 표기·로고 둘 다 없음)을 등급만으로 `noindex` 처리한 것이었다.
이 등급 226건 중 **202건은 본문이 800자 이상**으로 읽을 내용이 있었다. 색인 제외 기준을
등급이 아니라 본문 분량(`isNoindex`)으로 바꿔 202건을 색인에 복귀시켰다. 목록·홈·llms
노출 정책은 그대로다 — sitemap 브랜드 URL 1,541 → 1,743.

같은 작업에서 본문 안내 문구가 실제 `robots` 값과 어긋나던 것도 고쳤다(색인되는 페이지에
"검색 색인에서는 제외됩니다"라고 적혀 있었다).

남은 조치는 사람이 해야 한다 — 네이버 웹마스터 도구에서 새 URL 몇 건을 '웹 페이지 수집'으로
요청하면 색인 교체가 앞당겨진다. 진단 항목 자체는 색인 교체가 끝나면 사라진다.

## 7. 검토했으나 하지 않은 것

- **AI 전용 청킹·리라이팅** — 구글이 효과 없다고 명시.
- **`llms.txt` 확장** — 같은 이유. 현 수준 유지.
- **`DefinedTerm` 스키마 전환** — 사전 항목에 더 맞지만 `Article`+`Organization` 조합이
  이미 리치리절트와 AI 인용 양쪽에서 동작한다. 구조를 바꿀 만한 이득이 없다.
- **RSL 1.0** — 선택 사항, 실익 불분명.
