# brand-atlas 다음 작업 목록

> 갱신 2026-09-01. 근거는 `GEO-ANALYSIS.md`(GEO 점검) ·
> `.omc/plans/brand-atlas-geo-visibility-2026-09.md`(실행 계획).

## 전제

**유입은 사이트 이름이 아니라 브랜드명으로 온다.** "구찌 로고", "코치넬레 어느 나라"처럼
개별 브랜드 질의가 진입점이다. 8월 실측과도 일치한다 — 실제 클릭은 전부 정보 의도
롱테일에서 났고(CTR 33~100%), 브랜드명 헤드 키워드는 커머스가 구조적으로 점유한다.

따라서 우선순위는 **"브랜드 질의에 답할 수 있는 페이지를 늘리는 것"**이다.

### 지금 답할 수 있는 비율 (2026-09-01 실측, 총 1,449건)

| 질의 유형 | 답할 수 있는 페이지 | 비율 |
|---|---|---|
| `OO 브랜드` `OO 뜻` | 1,302 | 90% |
| `OO 로고` | 1,024 | 71% |
| `OO 역사` `OO 설립` | 897 | 62% |
| `OO 어느 나라` | 527 | 36% |
| `OO 설립연도` | 527 | 36% |
| `OO BI/CI` | 326 | 22% |

---

> 2026-09-07 갱신: 아래 1~4는 대부분 처리됨(CHANGELOG 2026-09-07). 팩트 표·질문형 H2·Wikidata 국가/설립연도 완료.
> 2차(같은 날): 로고 육안 검수 54건 채택, 등급 규칙 교정(publicReady 무시 → 디렉토리 231), 404 페이지.
> 남은 것: 한글명 로고 미보유 31건(닥터자르트·롬앤·아이더·탐앤탐스·더벤티·불스원 등) 수동 수집.

## 1. 로고 425건 보강 — 2026-09-07 일부 처리(로고 보유 1,004건, 한글명 있는 미보유 ~80건 남음)

- **여는 질의**: `OO 로고`, 이미지 검색
- **현재 71% → 목표 95%**
- 네이버 이미지 검색 API가 열려 있다(NAVER API HUB, `/search/v1/image`). 종전에는
  Playwright로 화면을 긁었다 — API가 더 안정적이고 빠르다.
- **주의**: 오매칭이 이 작업의 상시 위험이다. 2026-06 회차에서 잘못 붙은 로고를 감사해
  제거한 이력이 있다. 자동 채택 금지 — 후보를 모아 육안 검증하는 기존 방식을 유지한다.
- 대상 목록은 어드민 콘텐츠 탭 또는 `reports/admin-snapshot.json`의 `noLogo`.

## 2. 브랜드 팩트 표

- **여는 질의**: `OO 설립연도` `OO 본사` `OO 모기업` `OO 공식 사이트`
- 지금은 설립연도·본사·산업이 전부 산문에 녹아 있어 AI·검색엔진이 구조화해 뽑기 어렵다.
  브랜드 페이지에 표가 하나도 없다.
- 검증된 값만 넣는다. 팩트 3개 이상 보유 브랜드는 535건(37%)이고, 나머지는 표가 작아진다.
  없는 칸을 채우지 않는다.
- 빌더(`build-brand-pages.mjs`) 수정 → 1,449건 전량 자동 적용.

## 3. 질문형 H2

- **여는 질의**: `OO는 어느 나라 브랜드인가`
- 현재 H2가 전부 명사형(`한눈에 보는 브랜드`·`시작과 성장`)이라 질의와 직접 매칭되지 않는다.
  FAQ에는 질문형 H3가 5,487개 있는데 본문 헤딩에는 0개다.
- FAQ와 같은 규율을 적용한다 — 데이터로 답할 수 있는 항목만 질문형으로 바꾼다.
- 빌더 수정 → 전량 자동 적용.

## 4. 국가·설립연도 확대 (36% → 60%+)

- **여는 질의**: `OO 어느 나라` — 922개 페이지가 이 질의에 걸릴 근거가 없다.
- **경로**: 이미 연결된 위키데이터 408건에서 `P17`(국가)·`P571`(설립일)을 끌어온다.
  공식 웹사이트 URL 완전 일치로 확인된 개체이므로, 지금의 무할루시네이션 원칙을 깨지 않고
  늘릴 수 있는 유일한 경로다.
- 위키데이터 연결을 먼저 늘리면(현재 28%) 이 작업의 상한도 함께 올라간다.
  `officialWebsite` 미보유 843건에 공식 사이트를 채우는 것이 선행 과제.

## 5. 검색량 기반 우선순위 재정렬

- 지금은 1,449개를 전부 똑같이 취급한다 — 홈 노출도, 내부 링크도, 보강 순서도.
- 네이버 검색어트렌드 API(`/search-trend/v1/search`)로 수요를 재서 순서를 정한다.
  실측 예: 구찌 100 vs 코치넬레 0.27 — **약 300배 차이**.
- 어드민 키워드 탭에서 이미 조회할 수 있다.

## 6. 홈 Organization 스키마 + 소개 페이지 (최소한만)

- **유입 목적이 아니다.** 아무도 사이트명으로 검색하지 않는다.
- AI가 "구찌는 어느 나라"에 답할 때 **어느 출처를 인용할지 고르는 기준**이다. GPTBot이
  30일간 893회 읽어 갔지만, 발행 주체가 불분명하면 인용되지 않고 학습 재료로만 쓰인다.
- 현재 홈 스키마는 `WebSite` + `SearchAction`뿐이라 "누가 만드는가"를 선언하는 곳이 없다.
- 위키백과 등재·언급 늘리기는 **하지 않는다**(투입 대비 회수가 불확실).

## 7. 콘텐츠 얇은 44건 보강

- 렌더 본문 700자 미만이라 `noindex,follow` 처리된 페이지. 링크 그래프에는 남아 있다.
- 목록은 어드민 콘텐츠 탭.

---

## 관측 대기 (사람이 할 일 없음)

| 시점 | 확인할 것 | 오늘 기준선 |
|---|---|---|
| 2026-09-02 | GA4 정식 보고서 채워짐 | 실시간만 동작 |
| 2026-09-08 전후 | 허브 색인이 계속 오르는지 | 26개 중 25 |
| 2026-09-15~29 | 신규 slug 색인률 | 349/1,405 = 24.8% |
| 〃 | 구 slug 소멸 | 83건 잔존 |
| 〃 | 네이버 Yeti 방문 증가 | 30일 737회 (구글봇 1,518) |

주간 자동화(`deploy/weekly-refresh.sh`, cron 월 05:10)가 재빌드·검증·배포·색인 측정을
돌린다. 수용기준 미달이면 배포하지 않는다.

---

## 검토했으나 하지 않기로 한 것

- **Bytespider·AhrefsBot 차단** — 30일 4,645회로 트래픽 상위인데 검색 노출 기여는 없다.
  다만 지금 서버 부하가 문제되지 않아 보류. 부하가 생기면 robots.txt에서 막는다.
- **`llms.txt` 확장** — 구글이 검색 순위·노출에 영향 없음을 명시했다. 현 수준 유지.
- **RSL 1.0(AI 라이선스)** — 선택 사항. 도입 실익 불분명.
- **AI 전용 콘텐츠 청킹·리라이팅** — 구글이 효과 없다고 명시.
